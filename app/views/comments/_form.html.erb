<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :video_id %>
    <%= form.text_field :video_id, id: :comment_video_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :comment_user_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.trix_editor :body, id: :comment_body %>
  </div>

  <div class="field">
    <%= form.label :current_duration %>
    <%= form.text_field :current_duration, id: :comment_current_duration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
