<%= form_with(model: feature, local: true) do |form| %>
  <% if feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feature.errors.count, "error") %> prohibited this feature from being saved:</h2>

      <ul>
      <% feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.text_field :artist_id, id: :feature_artist_id %>
  </div>

  <div class="field">
    <%= form.label :video_id %>
    <%= form.text_field :video_id, id: :feature_video_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
